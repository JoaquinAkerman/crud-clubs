<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{club.name}}</title>
  </head>
  <body>
    <h1>{{club.name}}</h1>
    <img src="{{club.crestUrl}}" alt="{{club.name}}"><br>
    <p><strong>Nombre corto:</strong> {{club.shortName}}</p>
    <p><strong>Siglas:</strong> {{club.tla}}</p>
    <p><strong>Dirección:</strong> {{club.address}}</p>
    <p><strong>Teléfono:</strong> {{club.phone}}</p>
    <p><strong>Sitio web:</strong> <a href="{{club.website}}">{{club.website}}</a></p>
    <p><strong>Correo electrónico:</strong> {{club.email}}</p>
    <p><strong>Año de fundación:</strong> {{club.founded}}</p>
    <p><strong>Colores del club:</strong> {{club.clubColors}}</p>
    <p><strong>Estadio:</strong> {{club.venue}}</p>

    <div id="map"></div>

    <script>
      function initMap() {
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 16,
          center: {lat: 51.5560625, lng: -0.1086381}
        });

        var marker = new google.maps.Marker({
          position: {lat: 51.5560625, lng: -0.1086381},
          map: map,
          title: "{{club.name}}"
        });
      }
    </script>
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDhVsgsI5u9FHf50GT3DVZKEeF4RuzZKMo&callback=initMap" async defer></script>
  </body>
</html>
